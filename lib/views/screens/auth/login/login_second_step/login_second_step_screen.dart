
import 'package:flutter/material.dart';

class LoginSecondStepScreen extends StatelessWidget {
  const LoginSecondStepScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}